{
  "name": "gathering",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "concurrently \"bun run --cwd backend dev\" \"bun run --cwd frontend dev\"",
    "dev:elysia": "concurrently \"bun run --cwd backend dev:elysia\" \"bun run --cwd frontend dev\"",
    "install:all": "bun install && bun install --cwd backend && bun install --cwd frontend"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
